# **발행 — ○○○**

# **뉴스레터 에이전트 — 발행 단계 개선 제안**

2026-09-14 · 실측 기반

## **한 줄 요약**

발행 실패를 감지하는 코드가 없습니다. 웹훅이 죽어도 메트릭에는 '발행됨'으로 찍히고, 조용히 성공한 척합니다.

## **측정한 현재 상태**

| **항목** | **값** | **비고** |
| --- | --- | --- |
| 전송 방식 | 웹훅 1회 POST | 재시도 없음 |
| 실패 감지 | 상태코드 그룹만 판단 | 200·204 외는 로그 한 줄 |
| 실패 시 저장 | `발행: 실패` 로그만 | 알림·재발행 경로 없음 |
| 메트릭 | `published = 검수 통과 수` | 실제 성공 여부와 무관 |
| 기사 초과 시 | 뒤에서부터 `pop()` | 3건째면 그대로 누락 |
| 글자 초과 시 | 앞으로 이동, 뒤에서 삭제 | 잘린다는 표시 없음 |
| 푸터 시각 | `at`(UTC) 원본 출력 | KST 웹훅 채널에 9시간 차이 |

## **🔴 1. 실패한 발행이 성공으로 기록됩니다**

`send()`는 상태코드를 보고 로그 한 줄만 남깁니다. `metrics.jsonl`의 `published`는 `len(verified)`라서, **웹훅이 500을 줘도 발행된 것처럼 집계**됩니다.

```
웹훅 500 → send 실패 → 하지만 metrics: published = 5
```

**처방**: `send()`가 성공 시 `True`를 반환하는 점을 이용해, 실패한 `run_id`를 `store/failures.jsonl`에 남기고 상태코드를 저장하세요. 메트릭은 성공 건만 셉니다.

## **🔴 2. 재시도가 없습니다**

네트워크·Discord 일시 오류(429/5xx)는 한 방이면 끝입니다. 스케줄이 돌 때 한 번 실패하면 그날 브리핑은 유실입니다.

**처방**: 상태코드에 따라 2회 재시도(지수 백오프), 그래도 실패하면 실패 로그 + 다음 날 알림 채널에 알림.

## **🟡 3. 기사가 많아지면 뒤에서 조용히 잘립니다**

`EMBED_MAX=10`, `TOTAL_MAX=5800` 초과 시 `embeds.pop()`으로 뒤에서부터 삭제합니다. **가장 최근 추가된 기사가 가장 먼저 사라지므로**, 5건 이상 뽑히면 독자는 뒷 기사를 모릅니다.

**처방**: 살아남은 기사 수를 푸터(`N/총`)에 표시하고, 삭제된 기사의 제목을 리드에 "외 2건"으로 명시하세요. 선택은 5건 고정이라 초과 시 그대로 두어도 무방(임계값만 늘리면 됨).

## **🟡 4. 푸터 시각이 UTC입니다**

기사 `at`이 UTC 스탬프라 `%m-%d %H:%M`이 **UTC 시각**입니다. KST 채널에서 9시간 밀린 시간이 찍힙니다.

**처방**: `at.astimezone()` 또는 `TZ=Asia/Seoul` 환경에서 `strftime`을 KST로 통일하세요.

## **🟡 5. 같은 날 재실행하면 같은 글을 또 보냅니다**

수집·검수만 통과하면 웹훅으로 그대로 나갑니다. 발행 이력이 없어 **두 번 돌리면 이중 발행**입니다.

**처방**: 발행 성공한 URL을 `store/published.log`에 남기고, 같은 `run` 재실행 시 스킵하세요. (자료수집 제안 2번과 같은 이력 체계로 통일 가능)

## **우선순위**

|  | **제안** | **영향** | **소요** |
| --- | --- | --- | --- |
| 🔴 | 1. 실패를 메트릭에 반영 + 실패 로그 | 지표 신뢰도 | 10분 |
| 🔴 | 2. 재시도(2회, 백오프) | 발행 유실 방지 | 10분 |
| 🟡 | 3. 잘린 기사 표시 | 전달 누락 방지 | 10분 |
| 🟡 | 4. KST 푸터 | 시각 오표기 방지 | 5분 |
| 🟡 | 5. 발행 이력으로 재발행 방지 | 중복 발행 방지 | 10분 |

추가 검토: `lead` 문단을 루브릭 요약으로 대체(현재 "N건 골랐습니다" 안내형), 웹훅 secret 교체 주기.