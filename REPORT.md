# 서울·경기 부동산 브리핑 에이전트 — 수행 결과 보고서

분야: 서울·경기 부동산 (개발진행·거래/시세·정책·세금) · 실행일: 2026-09-15 · 모델: gpt-4.1-mini

## 1. 분야 및 독자 정의

**분야**는 서울·경기 부동산 4축이다: ① 개발 진행 상황
(재개발·재건축·GTX·신도시), ② 거래 및 시세 (실거래·전세·매매),
③ 부동산 정책 (대출·청약·규제지역), ④ 부동산 세금
(취득·보유·양도). 에이전트의 페르소나는 **부동산 전문가**로,
단순 요약이 아니라 "이번 주 뭘 체크해야 하는지" 코멘트(`why`)를 단다.

**타깃 독자 페르소나** (`audience.yaml`):

- 누구: 서울·경기에서 내 집 마련·갈아타기·투자를 고민하는 실수요자와 투자자
- 이미 아는 것: 청약 기본 구조, LTV·DSR 개념, 취득·보유·양도세의 큰 틀
- 그래서: 기초 해설은 버리고, 당장 돈·타이밍에 영향을 주는 소식만 남긴다.

## 2. 소스 채택표

지정 제외 6종(OpenAI·DeepMind·TechCrunch·The Verge·MIT TR·AI타임스)은
후보 단계에서 원천 제외했다. 제출 코드(`graph.py`·`run.py`·`audience.yaml`·
`metrics.py`·`REPORT.md`·`requirements.txt`) 전체를 URL 패턴으로
스캔해도 해당 도메인이 단 1건도 나오지 않는다.

### 2-1. 포털 직접 수집 조사 (요청 반영, 실측 결과)

| 포털 | 방식 | 실측 | 판정 |
| --- | --- | --- | --- |
| Google 뉴스 | RSS 쿼리 5종 (`news.google.com/rss/search`) | 쿼리당 약 100건, 정상 | ⚠️ 발견용으로만 가능 (아래) |
| Naver | Search API (`openapi.../news.json`, display=100·sort=date) | 키 입력 완료, 인증 실패(024) 조사 중 | ⏳ 키 통과 시 6번째 경로 (아래) |
| Daum | 공개 RSS/API | 없음 | ❌ 스크래핑뿐이라 제외 (차단·약관 리스크) |
| MSN | `/ko-kr/news/rss`, `/ko-kr/feed` | 404 / 빈 피드 | ❌ 공개 RSS 없음 |

Google 뉴스 RSS는 두 가지 결격이 실측됐다.
첫째, 시간 분포가 일간 브리핑에 부적합하다
(쿼리 중앙값이 수일~수개월 전, 24h 내 약 11건).
`after:YYYY-MM-DD` 연산자로 완화는 되지만,
둘째, 기사 링크가 `news.google.com` 리다이렉트라 서버 요청이
**HTTP 400**으로 차단된다 (브라우저 헤더 포함 2회 재현).
원문이 없으면 검수(4단계)의 근거가 성립하지 않으므로,
본 수집원은 **원문 URL이 곧바로 열리는 직접 언론사 RSS 5곳**으로 확정했다.
Naver Search API(무료, 키 발급 5분)는 키가 준비되는 대로 6번째 경로로
추가할 수 있게 `outlet` 필드·표시 로직을 미리 분리해 두었다.

### 2-1b. Naver 6번째 경로 (구현 완료, 키 인증 대기)

- 쿼리 5종(개발·거래·정책·세금·경기) × 1호출(`display=100, sort=date`),
  회당 최대 5호출. 링크는 `originallink`(언론사 원문 직결) 우선이라
  본문 추출·검수 체계가 그대로 동작한다.
- **80% 한도 가드**: 무료 한도 25,000건/일 → 20,000건 도달 시 Naver 경로
  전체 스킵 (`store/naver_usage.json` 일별 카운터, KST 자정 리셋.
  Naver가 잔여량 헤더를 주지 않아 로컬 카운터가 유일한 수단이다).
- 키 없음·한도 도달·쿼리 실패 모두 `dead` 로그 한 줄로 남고 RSS 5곳은
  정상 진행한다. 2026-09-15 최신 dry-run에서 Naver 5쿼리 401 실패에도
  `240 → 48 → 3 → 3 → dry-run` 전 단계가 정상 완료되어 예외 처리가 실증됐다.
- 가드 단위 테스트 5항목 통과 (초기 20000·차감·80% 정지·자정 리셋·401 처리).
- 현재 상태: 키 입력은 완료됐으나 Naver가 `024 인증 실패`를 반환해
  경로가 비활성 상태다. 키가 통과되는 실행부터 자동 합류하며, 그 전까지
  RSS 5곳만으로 브리핑이 발행된다 (서비스 중단 없음).

### 2-2. 직접 RSS 채택표 (2026-09-15 실측)

| 소스 | RSS | 72h 건수 | 부동산 적중률 | 판정 | 근거 |
| --- | --- | --- | --- | --- | --- |
| 하우징헤럴드 | housingherald.co.kr/rss/allArticle.xml | 30 | 25/30 (83%) | ✅ 채택 | 정비사업 전문, 개발진행 전담 |
| 한경부동산 | hankyung.com/feed/realestate | 36 | 17/36 (47%) | ✅ 채택 | 부동산 섹션 피드, 거래·정책 균형 |
| 동아경제 | rss.donga.com/economy.xml | 50 | 11/50 (22%) | ✅ 채택 | 제2 종합지, 한경과 교차검증 |
| 매경 | mk.co.kr/rss/30000001 | 50 | 3/50 (6%) | ✅ 채택 | 발행량·속보 커버 (예선 구조로 희석 흡수) |
| 연합경제 | yna.co.kr/rss/economy.xml | 120 | 7/120 (6%) | ✅ 채택 | 정부발표 속보가 가장 빠름 |
| 국토일보 | ikld.kr/rss/allArticle.xml | 50 | 5/50 (10%) | ❌ 탈락 | 건설·기타 희석, 전문지 역할은 헤럴드가 커버 |
| 한겨레 | hani.co.kr/rss | 30 | - | ❌ 탈락 | 날짜 필드 없음 → 시간창 필터 불가 |
| 서울경제·아시아경제·이데일리·머니투데이·국토부·서울시 | 각 RSS 추정 경로 | 0 | - | ❌ 탈락 | 404 또는 빈 피드 |

매 실행 `수집 N건 (소스별 건수)` 로그가 증거다 (5절).

## 3. 선별 로직 설계

**중요도 판단 문장** (그대로 LLM 선별 프롬프트가 된다):

> 서울·경기 개발 진행이 거주·자산에 직접 영향을 주나 /
> 실거래·시세 변동이 매수·매도 타이밍 판단에 쓸모 있나 /
> 대출·청약·규제 변경이 지금 당장 적용되나 /
> 세금 변경으로 내야 할 돈이 바뀌나

**예선/본선 2단계 + Batch 크기 이유** (`BATCH=40, PRE=8, TARGET=5`):

- 수집 창 24h 실측 약 242건 (연합 120 쏠림) → 수집 후 **소스 라운드로빈
  재배열**을 먼저 걸어 예선 각 묶음에 5개 소스가 고루 섞이게 한다.
- 40건 묶음마다 8건씩 예선 통과 → 본선에서 5건으로 압축.
- `event`는 4개 카테고리(개발·정비 / 거래·시세 / 정책·규제 / 세금·대출)로
  강제한다. 같은 사건은 같은 라벨 1건만 남기고, 카테고리는 Discord
  embed 색상과 1:1로 매핑된다 (프롬프트 이탈 시 `정책·규제`로 정규화).

**동작 근거**: `store/metrics.jsonl` 매 행의 `select_reasons`
(제목·경로·언론사·선별이유·event). 2026-09-15 실발행 회차:

- `[한경부동산] '청량리역~수원역 30분' GTX` → (개발·정비)
  "광역 교통망 개선으로 실거주와 자산가치에 직접적 영향"
- `[한경부동산] 서울 토지거래허가 신청 3012건 급감` → (정책·규제)
  "대출규제·금리·세제 개편이 즉시 영향을 주는 사례"
- `[한경부동산] 실거래가로 증명된 호수 프리미엄` → (거래·시세)
  "매수·매도 타이밍 판단에 유용한 시세 정보"

## 4. 파이프라인 구조도

```mermaid
flowchart TD
    S((START)) --> C[collect<br/>직접 RSS 5곳 + Naver API 5쿼리(키·80% 한도 조건부)<br/>24h 창 · URL 중복제거 · 소스 라운드로빈 재배열]
    C --> L[select<br/>예선 40건→8건 · 본선→5건<br/>reason + 4카테고리 event · 중복사건 제거]
    L -- Send 기사별 fan-out --> R[report<br/>본문 추출 3단 체인→headline/summary/why 전문가 코멘트<br/>600자 미만 제외]
    R --> V[verify<br/>①숫자 교차검증 ②LLM 근거판정<br/>실패시 1회 재생성, 그래도 실패면 스킵]
    V --> P[publish<br/>Discord embeds 카테고리 색상 · KST 표기<br/>실패시 2회 재시도 + failures.jsonl]
    P --> E((END))
    V -. 로그 .-> M[(store/metrics.jsonl<br/>counts + select_reasons + verify_notes)]
```

State(`Brief`): `hours / collected / picked / drafted / verified /
verify_notes / log`. `.github/workflows/daily.yml`로 매일 KST 07:30 자동 실행.

## 5. 실행 기록

최신 4회차 (부동산 파이프라인, `store/metrics.jsonl` 원본 보존):

```
수집 → 선별 → 취재 → 발행
  242 →   3 →   3 →   3  (dry-run)
  242 →   3 →   3 →   3  (sent — Discord 발행 성공)
  242 →   3 →   3 →   3  (dry-run, Naver 키 없음 스킵)
  240 →   3 →   3 →   3  (dry-run, Naver 5쿼리 401에도 전 단계 정상)
```

대표 로그 (실발행 회차):

```
① 수집   24시간 창 · 242건 (하우징헤럴드 1, 한경부동산 21, 동아경제 50, 매경 50, 연합경제 120)
② 선별   242 → 예선 51 → 3건
   취재 완료 한경부동산 · 1644자(fetch)
   취재 완료 한경부동산 · 1686자(fetch)
   취재 완료 한경부동산 · 4690자(fetch)
④ 검수   3 → 3건
⑤ 발행   3건 · sent
```

읽는 법:

- **수집 타당성**: 5개 경로 모두 기여 로그, 라운드로빈으로 연합 쏠림 흡수.
  단, 최종 3건이 모두 한경부동산에 몰렸다. 소스별 발행 기여 표
  (`metrics.py`)에 0 기여 소스가 그대로 노출되므로 편중이 숨겨지지 않는다.
  원인 가설: 당일 부동산 뉴스 중 한경 섹션 피드의 적중률이 가장 높았던
  데다, 예선이 배치 순서 효과를 완전히 못 걷어낸 탓이다.
- **검수 증명** (`verify_probe.py` 재현 가능): 숫자 교차검증이 변조 금액
  적발·정상 통과, LLM 판정이 `ok=False+지적 / ok=True`로 일치.
  숫자 검사는 요약뿐 아니라 전문가 코멘트(`why`)까지 함께 검사한다.
  당일 verify_notes 빈 기록 = 전건 1차 통과.
- **발행 증명**: `⑤ 발행 3건 · sent` + Discord 채널 수신 화면을 아래에 첨부한다.

> 📎 발행 캡처: Discord "서울경기 부동산 브리핑" 채널, 2026-09-15 12:17
> 수신 확인 — 리드 1 + 기사 3 embeds (① GTX-C노선 2031년 개통 예정 /
> ② 서울 아파트 토지거래허가 신청 3012건 / ③ 실거래가 호수 프리미엄).
> 캡처 이미지: `스크린샷 2026-09-15 151532.png` (별첨).

## 6. 프로젝트 회고

**가장 공들인 부분 — "포털에서 수집"을 말 그대로가 아니라 동작하게 만들기.**

- Google 뉴스 RSS·MSN·Daum을 전부 실측하고, Google 본문 400 차단을
  브라우저 헤더까지 바꿔가며 2회 재현한 뒤 미련 없이 버렸다.
  안 되는 경로를 보고서에 수치로 남기는 게 타당한 탈락 기준이다.
- 대신 242건 수집의 쏠림(연합 120건)을 라운드로빈 재배열로 흡수하고,
  event를 4카테고리로 강제해 선별·색상·지표를 한 줄로 꿰었다.

**향후 보완점.**

1. Naver 키 인증: `024` 해결되는 대로 다음 실행부터 Naver 5쿼리 합류
   (코드·가드·리셋 로직 모두 준비됨).
2. 선별 편중 완화: 당일 3건이 한경에 몰린 만큼, 소스 가중치(예: 경로당
   본선 최소 1석) 또는 임베딩 유사도 중복제거를 검토한다.
3. 하우징헤럴드(주간지 성격, 24h 1건)는 24h 창에서 존재감이 낮다.
   전문지 가중이나 72h 별도 창을 검토한다.
